public class ConcatenatingStringsTest {

	/**
	 * Write two tests. One that verifies the '+' operator for concatenating
	 * a String and another that uses the concat method from the String class.
	 */
}
